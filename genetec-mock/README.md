

To run this mock, 

`./gradlew bootRun`

Alternatively, you can spin up a simpler mock with stoplight prism:

`# install prism first: npm install -g @stoplight/prism-cli`
`prism mock genetec-identity-v4-swagger.json`

